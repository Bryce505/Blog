# 剪藏栏目 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `Bryce505/Notes` 的公众号与 X 月度剪藏索引同步进本仓库，在 `/clips` 一页按剪藏时间倒序展示，可按来源与优先级筛选。

**Architecture:** 同步 workflow 里跑 `pipeline/clips.py` 把上游 md 解析成 `src/data/clips.json`（唯一产物，提交进仓库），站点侧一句 `import` 读进来渲染，构建期零解析逻辑。解析器是这次唯一有分支的代码，用 `pipeline/test_pipeline.py` 覆盖。

**Tech Stack:** Python 3.11（标准库，零第三方依赖）、Astro 7.2.6、GitHub Actions

**Spec:** `docs/superpowers/specs/2026-09-05-剪藏栏目-design.md`

## Global Constraints

- **零第三方依赖**：`clips.py` 只用标准库（`argparse` / `json` / `posixpath` / `re` / `pathlib`），不往 `pipeline/requirements.txt` 加任何东西
- **自检零框架**：新增自检写成 `def test_xxx():` + `assert`，放进 `pipeline/test_pipeline.py`，靠文件底部已有的 runner 跑
- **注释写「为什么」不写「做了什么」**（`CLAUDE.md`）。带具体数字的判断要有实测依据
- **中文注释与文档**，术语保留英文原词
- **不碰 `pipeline/` 现有任何文件的行为**：`clips.py` 是独立模块，不进 `main.py` 的五步流程，不经过 `verify.py`
- **不碰 `posts` collection、`published.json`、RSS、标签页、归档页**
- CSS 只用已有令牌：`--bg` `--surface` `--fg` `--muted` `--border` `--accent` `--serif` `--sans` `--mono` `--measure`；工具类 `.wrap` `.num` `.eyebrow`
- 一个分支一篇开发日志（`CLAUDE.md`），最后一个任务统一补
- **上游格式已实测核对过**：68 条真实条目（`notes/2026-08.md` 51 条、`notes/2026-09.md` 2 条、`notes/x/2026-08.md` 15 条）全部匹配本计划里的正则；优先级取值只有 `高`(51)/`中`(10)/`低`(7)；`洞见` 缺 2 条，其余字段 68 条全有；无多行字段，无缺快照

---

### Task 1: 解析器 `pipeline/clips.py` 与自检

**Files:**
- Create: `pipeline/clips.py`
- Create: `pipeline/fixtures/clips-weixin.md`
- Create: `pipeline/fixtures/clips-x.md`
- Modify: `pipeline/test_pipeline.py`（顶部 import 区加 `import clips as cl`；文件末尾 runner 之前加一节自检）

**Interfaces:**
- Consumes: 无（首个任务）
- Produces:
  - `clips.parse_month(text: str, source: str, month: str, notes_path: str) -> list[dict]`
  - `clips.collect(notes_dir: Path) -> list[dict]`（按 `clippedAt` 倒序）
  - `clips.main(argv=None) -> None`（`--notes` / `--out`）
  - 条目字典的键：`source` `month` `title` `clippedAt` `publishedAt` `priority` `keywords` `summary` `insights` `verdict` `url` `snapshot`

- [ ] **Step 1: 写两份 fixture**

`pipeline/fixtures/clips-weixin.md` —— 三条，分别覆盖「全字段」「关键词里带斜杠」「缺洞见」：

```markdown
# 2026年08月

## 最新版白皮书：抗体偶联药物（ADC）的生物分析（上）
- **剪藏**：2026-08-29 07:25 ｜ **发布**：2026-07-22
- **优先级**：高 ｜ **状态**：未读
- **关键词**：抗体偶联药物 / 生物分析 / LC-MS/MS / 游离载荷
- **摘要**：系统介绍了 ADC 生物分析的关键策略和技术平台。
- **洞见**：
  - 杂交免疫捕获 LC-MS/MS 正逐渐取代传统 LBA。
  - 总抗体与 ADC 浓度联合测定可推算平均 DAR。
- **是否值得读**：内容系统详尽，对 ADC 研发人员有高度参考价值。
- **链接**：[原文](https://mp.weixin.qq.com/s/BywLHNiFo_eIdlck8lckPg) ｜ [全文快照](../archive/2026-08/%E6%9C%80%E6%96%B0%E7%89%88-BywLHN.md)

## 发布时间拿不到的那种
- **剪藏**：2026-08-28 09:00 ｜ **发布**：未知
- **优先级**：中 ｜ **状态**：未读
- **关键词**：无
- **摘要**：一条上游没抓到发布时间、也没有关键词的记录。
- **是否值得读**：留着测边界。
- **链接**：[原文](https://mp.weixin.qq.com/s/AAAAAA) ｜ [全文快照](../archive/2026-08/bian-jie-AAAAAA.md)

## CMC常用的DoE方法
- **剪藏**：2026-08-27 12:35 ｜ **发布**：2026-08-27
- **优先级**：低 ｜ **状态**：未读
- **关键词**：DoE / CMC
- **摘要**：介绍了 CMC 研究中常用的六种试验设计方法。
- **是否值得读**：常规专业科普，无特别独到的观点。
- **链接**：[原文](https://mp.weixin.qq.com/s/yro-3usR8TSAL2onN4RnFg) ｜ [全文快照](../archive/2026-08/CMC-yro-3u.md)
```

`pipeline/fixtures/clips-x.md` —— 一条，快照路径比公众号多退一层：

```markdown
# 2026年08月

## Agent = Model + Harness：六层实战手册
- **剪藏**：2026-08-28 18:16 ｜ **发布**：2026-08-28
- **优先级**：高 ｜ **状态**：未读
- **关键词**：Harness / Agent工程 / 六层架构
- **摘要**：文章提出 Agent = Model + Harness 的核心观点。
- **洞见**：
  - 只改 Harness 不换模型，收益可超过模型升级。
- **是否值得读**：信息密度高，有直接可操作的指导价值。
- **链接**：[原文](https://x.com/shao__meng/status/2093228362965651665) ｜ [全文快照](../../archive/x/2026-08/Agent-651665.md)
```

- [ ] **Step 2: 写失败的自检**

`pipeline/test_pipeline.py` 顶部 import 区加一行（保持现有的字母序风格）：

```python
import clips as cl
```

在文件末尾、`if __name__ == '__main__':` 之前加：

```python
# ---------- clips（剪藏栏目）----------

def _weixin():
    return cl.parse_month((FIX / 'clips-weixin.md').read_text(encoding='utf-8'),
                          'weixin', '2026-08', 'notes/2026-08.md')


def _x():
    return cl.parse_month((FIX / 'clips-x.md').read_text(encoding='utf-8'),
                          'x', '2026-08', 'notes/x/2026-08.md')


def test_clips_parses_every_field():
    """一条完整记录的十二个字段都要落到位。"""
    c = _weixin()[0]
    assert c['source'] == 'weixin' and c['month'] == '2026-08'
    assert c['title'] == '最新版白皮书：抗体偶联药物（ADC）的生物分析（上）'
    assert c['clippedAt'] == '2026-08-29 07:25'
    assert c['publishedAt'] == '2026-07-22'
    assert c['priority'] == '高'
    assert c['summary'].startswith('系统介绍了 ADC')
    assert len(c['insights']) == 2, c['insights']
    assert c['insights'][0].startswith('杂交免疫捕获')
    assert c['verdict'].startswith('内容系统详尽')
    assert c['url'] == 'https://mp.weixin.qq.com/s/BywLHNiFo_eIdlck8lckPg'


def test_clips_keyword_with_slash_stays_one_keyword():
    """关键词按 ' / ' 切，不按 '/' 切。

    真实语料里有 `LC-MS/MS`、`敲除/敲低` 这类自带斜杠的词，按裸斜杠切会把
    一个词劈成两个假关键词。
    """
    assert _weixin()[0]['keywords'] == ['抗体偶联药物', '生物分析', 'LC-MS/MS', '游离载荷']


def test_clips_optional_fields_may_be_absent():
    """洞见、发布时间、关键词缺失是上游的正常产出，不是错误。

    实测 68 条真实记录里 2 条没有洞见（AI 判定该帖没有实质观点）。
    """
    entries = _weixin()
    assert entries[1]['publishedAt'] == '', entries[1]     # 上游写「未知」
    assert entries[1]['keywords'] == [], entries[1]        # 上游写「无」
    assert entries[2]['insights'] == [], entries[2]        # 整行缺席


def test_clips_snapshot_relative_path_becomes_repo_url():
    """公众号退一层、X 退两层，都要还原成 Notes 仓库的 blob 链接。"""
    assert _weixin()[0]['snapshot'] == (
        'https://github.com/Bryce505/Notes/blob/master/'
        'archive/2026-08/%E6%9C%80%E6%96%B0%E7%89%88-BywLHN.md')
    assert _x()[0]['snapshot'] == (
        'https://github.com/Bryce505/Notes/blob/master/'
        'archive/x/2026-08/Agent-651665.md')


def test_clips_snapshot_path_is_not_encoded_twice():
    """上游已经 percent-encode 过，再编一次链接就打不开了。"""
    assert '%25' not in _weixin()[0]['snapshot']


def test_clips_missing_required_field_raises():
    """标题 / 剪藏时间 / 原文链接缺任一条，这张卡片就不成立 —— 抛错。"""
    full = (FIX / 'clips-x.md').read_text(encoding='utf-8')
    for drop in ('- **剪藏**：2026-08-28 18:16 ｜ **发布**：2026-08-28\n',
                 '- **链接**：[原文](https://x.com/shao__meng/status/2093228362965651665) ｜ [全文快照](../../archive/x/2026-08/Agent-651665.md)\n'):
        try:
            cl.parse_month(full.replace(drop, ''), 'x', '2026-08', 'notes/x/2026-08.md')
        except ValueError:
            continue
        assert False, f'缺了这行竟然没抛错：{drop!r}'


def test_clips_priority_outside_three_buckets_raises():
    """档外的优先级会让这条被页面上每一个筛选条件都藏起来，等于内容消失。"""
    bad = (FIX / 'clips-x.md').read_text(encoding='utf-8').replace(
        '- **优先级**：高 ｜', '- **优先级**：紧急 ｜')
    try:
        cl.parse_month(bad, 'x', '2026-08', 'notes/x/2026-08.md')
    except ValueError as e:
        assert '紧急' in str(e), e
    else:
        assert False, '档外优先级没抛错'


def test_clips_zero_entries_raises():
    """上游把 ## 改成 ###，逐条检查一条都不会触发 —— 靠这条兜底。

    不拦的话站上会静默变成一个空栏目，没有任何人会收到通知。
    """
    try:
        cl.parse_month('# 2026年08月\n\n### 标题层级变了\n- **摘要**：x\n',
                       'x', '2026-08', 'notes/x/2026-08.md')
    except ValueError as e:
        assert '一条' in str(e), e
    else:
        assert False, '零条目没抛错'


def test_clips_collect_merges_both_sources_newest_first():
    """两个来源合成一条流，按剪藏时间倒序。"""
    TMP.mkdir(parents=True, exist_ok=True)
    up = TMP / 'clips-upstream'
    shutil.rmtree(up, ignore_errors=True)
    (up / 'notes' / 'x').mkdir(parents=True)
    (up / 'notes' / '2026-08.md').write_text(
        (FIX / 'clips-weixin.md').read_text(encoding='utf-8'), encoding='utf-8')
    (up / 'notes' / 'x' / '2026-08.md').write_text(
        (FIX / 'clips-x.md').read_text(encoding='utf-8'), encoding='utf-8')

    got = cl.collect(up)
    assert len(got) == 4, got
    assert [c['clippedAt'] for c in got] == [
        '2026-08-29 07:25', '2026-08-28 18:16', '2026-08-28 09:00', '2026-08-27 12:35']
    # X 那条夹在两条公众号中间，证明是合并排序而不是先公众号后 X
    assert got[1]['source'] == 'x', got[1]
    assert got[1]['snapshot'].startswith(
        'https://github.com/Bryce505/Notes/blob/master/archive/x/')


def test_clips_collect_without_any_month_file_raises():
    """上游把 notes/ 挪走时要红一条，不能产出一个空 JSON 静默上线。"""
    TMP.mkdir(parents=True, exist_ok=True)
    empty = TMP / 'clips-empty'
    shutil.rmtree(empty, ignore_errors=True)
    empty.mkdir(parents=True)
    try:
        cl.collect(empty)
    except ValueError as e:
        assert 'notes' in str(e), e
    else:
        assert False, '上游一个月度文件都没有，竟然没抛错'
```

- [ ] **Step 3: 跑自检，确认它失败**

Run: `.venv/bin/python pipeline/test_pipeline.py 2>&1 | tail -20`
Expected: 因为 `pipeline/clips.py` 还不存在，import 阶段就 `ModuleNotFoundError: No module named 'clips'`

- [ ] **Step 4: 写 `pipeline/clips.py`**

```python
"""把 Bryce505/Notes 的月度剪藏索引解析成站点用的 JSON。

用法: python pipeline/clips.py --notes <Notes 仓库路径> --out src/data/clips.json

上游格式的唯一真相是 Notes 仓库的 `src/clipper/md_writer.py::render_entry` ——
那是机器生成的固定格式，所以这里用严格正则匹配而不是宽松扫描：匹配不上意味着
上游改了格式，那时候要的是 workflow 红一条，不是站上悄悄少几条内容。

本模块与 main.py 的五步流程完全无关，也不经过 verify.py —— 剪藏不是 AI 改写
本仓库笔记的产物，没有「把流速改错」那个风险面，校验器的两组检查在这里不适用。
"""

from __future__ import annotations

import argparse
import json
import posixpath
import re
from pathlib import Path

# 快照留在 Notes 仓库，本站只链过去：公众号正文是别人的版权内容，
# 转载到公网站点有风险；且 archive/ 一个月就几百 KB，镜像会持续撑大本仓库。
SNAPSHOT_BASE = 'https://github.com/Bryce505/Notes/blob/master/'

# 上游 ai.py 已经把优先级兜底成这三个值之一，出现第四种只可能是格式变了
PRIORITIES = ('高', '中', '低')

_HEADING = re.compile(r'(?m)^## (.+)$')
_CLIPPED = re.compile(r'^- \*\*剪藏\*\*：(.+?) ｜ \*\*发布\*\*：(.+)$')
_PRIORITY = re.compile(r'^- \*\*优先级\*\*：(.+?) ｜ \*\*状态\*\*：(.+)$')
_KEYWORDS = re.compile(r'^- \*\*关键词\*\*：(.+)$')
_SUMMARY = re.compile(r'^- \*\*摘要\*\*：(.+)$')
_VERDICT = re.compile(r'^- \*\*是否值得读\*\*：(.+)$')
_LINK = re.compile(r'^- \*\*链接\*\*：\[原文\]\((\S+?)\)(?: ｜ \[全文快照\]\((\S+?)\))?$')
_INSIGHT = re.compile(r'^  - (.+)$')


def parse_month(text: str, source: str, month: str, notes_path: str) -> list[dict]:
    """把一份月度索引解析成条目列表。

    `notes_path` 是这份索引在 Notes 仓库里的相对路径（如 `notes/x/2026-08.md`），
    既用来还原快照的相对链接，也用来让报错指得出是哪份文件。
    """
    parts = _HEADING.split(text)
    # split 的结果是 [标题前的内容, 标题1, 正文1, 标题2, 正文2, ...]
    entries = [_parse_entry(title, body, source, month, notes_path)
               for title, body in zip(parts[1::2], parts[2::2])]
    if not entries:
        raise ValueError(
            f'{notes_path} 一条剪藏都没解析出来 —— 上游多半改了条目标题的层级。'
            '别把这里改成静默返回空列表：逐条检查一条都不会触发，'
            '站上会变成一个没人会发现的空栏目。')
    return entries


def collect(notes_dir: Path) -> list[dict]:
    """扫上游的 notes/ 与 notes/x/，合并后按剪藏时间倒序。"""
    clips: list[dict] = []
    for source, sub in (('weixin', ''), ('x', 'x')):
        directory = notes_dir / 'notes' / sub
        for path in sorted(directory.glob('*.md')):
            clips += parse_month(path.read_text(encoding='utf-8'), source,
                                 path.stem, posixpath.join('notes', sub, path.name))
    if not clips:
        raise ValueError(
            f'{notes_dir} 下的 notes/ 一个月度索引都没找到 —— 上游挪了目录。'
            '宁可红一条，也不要产出一个空 JSON 把整栏抹掉。')
    # 'YYYY-MM-DD HH:MM' 定宽，字典序就是时间序，不必解析成 datetime
    clips.sort(key=lambda c: c['clippedAt'], reverse=True)
    return clips


def _parse_entry(title: str, body: str, source: str, month: str,
                 notes_path: str) -> dict:
    entry = {
        'source': source, 'month': month, 'title': title.strip(),
        'clippedAt': '', 'publishedAt': '', 'priority': '', 'keywords': [],
        'summary': '', 'insights': [], 'verdict': '', 'url': '', 'snapshot': '',
    }
    lines = body.split('\n')
    for i, line in enumerate(lines):
        if m := _CLIPPED.match(line):
            entry['clippedAt'] = m.group(1).strip()
            published = m.group(2).strip()
            # 上游抓不到发布时间时写「未知」，落到站上就是不显示这一项
            entry['publishedAt'] = '' if published == '未知' else published
        elif m := _PRIORITY.match(line):
            entry['priority'] = m.group(1).strip()
        elif m := _KEYWORDS.match(line):
            entry['keywords'] = _split_keywords(m.group(1).strip())
        elif m := _SUMMARY.match(line):
            entry['summary'] = m.group(1).strip()
        elif m := _VERDICT.match(line):
            entry['verdict'] = m.group(1).strip()
        elif m := _LINK.match(line):
            entry['url'] = m.group(1)
            if m.group(2):
                entry['snapshot'] = _snapshot_url(m.group(2), notes_path)
        elif line.startswith('- **洞见**'):
            entry['insights'] = _take_insights(lines[i + 1:])
    _require(entry, notes_path)
    return entry


def _split_keywords(raw: str) -> list[str]:
    """按 ' / ' 切，不按 '/' 切。

    真实语料里有 `LC-MS/MS`、`敲除/敲低` 这类自带斜杠的词，按裸斜杠切会把一个
    词劈成两个假关键词。上游 md_writer 用的正是 ' / '.join()。
    """
    if raw == '无':          # 上游对空关键词的写法
        return []
    return [k.strip() for k in raw.split(' / ') if k.strip()]


def _take_insights(rest: list[str]) -> list[str]:
    """「- **洞见**：」之后连续的缩进列表项，遇到第一个不匹配的行就停。"""
    out = []
    for line in rest:
        m = _INSIGHT.match(line)
        if not m:
            break
        out.append(m.group(1).strip())
    return out


def _snapshot_url(relative: str, notes_path: str) -> str:
    """相对索引文件的路径 → Notes 仓库的 blob URL。

    上游写的是相对路径（公众号 `../archive/…`，X 的索引在 notes/x/ 下要多退
    一层 `../../archive/x/…`），且中文已经 percent-encode 过。这里只做路径
    拼接，绝不再编码一次 —— 二次编码会把 %E4%B8%87 变成 %25E4%25B8%2587，
    链接直接打不开。
    """
    path = posixpath.normpath(
        posixpath.join(posixpath.dirname(notes_path), relative))
    return SNAPSHOT_BASE + path


def _require(entry: dict, notes_path: str) -> None:
    """必填字段的界线是「缺了这一条，这张卡片还成不成立」。"""
    name = entry['title'] or '无标题'
    for field in ('title', 'clippedAt', 'url'):
        # 标题与链接缺了是一张点不开的无名卡片；剪藏时间缺了排不进时间流
        if not entry[field]:
            raise ValueError(f'{notes_path} 的《{name}》缺 {field}，上游格式变了')
    # 档外的优先级会让这条卡片被页面上每一个筛选条件都藏起来，等于内容凭空消失
    if entry['priority'] not in PRIORITIES:
        raise ValueError(
            f'{notes_path} 的《{name}》优先级是 {entry["priority"]!r}，'
            f'不在 {PRIORITIES} 里 —— 上游格式变了')


def main(argv=None) -> None:
    ap = argparse.ArgumentParser(description='把 Notes 的月度剪藏索引解析成站点 JSON')
    ap.add_argument('--notes', required=True, help='Notes 仓库的 checkout 路径')
    ap.add_argument('--out', required=True, help='输出 JSON 路径')
    args = ap.parse_args(argv)

    clips = collect(Path(args.notes))
    out = Path(args.out)
    out.parent.mkdir(parents=True, exist_ok=True)
    # ensure_ascii=False：中文不转义，diff 才看得出上游改了什么
    out.write_text(json.dumps(clips, ensure_ascii=False, indent=2) + '\n',
                   encoding='utf-8')
    print(f'{len(clips)} 条剪藏 → {out}')


if __name__ == '__main__':
    main()
```

- [ ] **Step 5: 跑自检，确认全绿**

Run: `.venv/bin/python pipeline/test_pipeline.py 2>&1 | tail -15`
Expected: 末行 `N/N passed`，N = 之前的 152 + 新增的 10 = 162

- [ ] **Step 6: 提交**

```bash
git add pipeline/clips.py pipeline/fixtures/clips-weixin.md pipeline/fixtures/clips-x.md pipeline/test_pipeline.py
git commit -m "剪藏解析器：把 Notes 的月度索引解析成 clips.json"
```

---

### Task 2: 拿真实上游数据实跑，生成 `src/data/clips.json`

**Files:**
- Create: `src/data/clips.json`（解析器产出，不手写）

**Interfaces:**
- Consumes: `clips.main(['--notes', ..., '--out', ...])`
- Produces: `src/data/clips.json`，供 Task 4 的页面 import

- [ ] **Step 1: clone 上游并实跑**

```bash
git clone --depth 1 https://github.com/Bryce505/Notes /tmp/notes-upstream
.venv/bin/python pipeline/clips.py --notes /tmp/notes-upstream --out src/data/clips.json
```

Expected: 打印 `68 条剪藏 → src/data/clips.json`（条数以实际输出为准，会随上游增长）

- [ ] **Step 2: 抽查产出**

```bash
python3 -c "
import json;d=json.load(open('src/data/clips.json'))
print('条数', len(d))
print('来源分布', {s: sum(1 for c in d if c['source']==s) for s in ('weixin','x')})
print('优先级分布', {p: sum(1 for c in d if c['priority']==p) for p in ('高','中','低')})
print('倒序', [c['clippedAt'] for c in d[:3]])
print('无快照', sum(1 for c in d if not c['snapshot']))
print('二次编码', sum(1 for c in d if '%25' in c['snapshot']))
"
```

Expected: 来源分布 `weixin` 53 / `x` 15；优先级 51/10/7；`clippedAt` 严格倒序；无快照 0；二次编码 0

- [ ] **Step 3: 随机点开一个快照链接确认不是死链**

```bash
python3 -c "import json;print(json.load(open('src/data/clips.json'))[0]['snapshot'])"
curl -s -o /dev/null -w '%{http_code}\n' -L "$(python3 -c "import json;print(json.load(open('src/data/clips.json'))[0]['snapshot'])")"
```

Expected: `200`

- [ ] **Step 4: 提交**

```bash
git add src/data/clips.json
git commit -m "首次同步剪藏数据：68 条（公众号 53 / X 15）"
```

---

### Task 3: 视觉稿

**Files:**
- Create: `design/Clips.dc.html`
- Modify: `design/canvas.json`（`artboards` 加一块，`page-1`）

**Interfaces:**
- Consumes: `design/_tokens.txt` 记录的令牌 = `src/styles/global.css` 的 `:root`
- Produces: 页面实现（Task 4）照抄的版式与配色决策

- [ ] **Step 1: 用 `design` 技能出「剪藏」栏目页画板**

画板尺寸 1080×1600，落 `design/Clips.dc.html`，内容按 spec 第 6 节：页头说明三件事、工具条两组筛选 + 条数、月份分组小标题、条目卡片七层结构。颜色只用 `global.css` 的令牌值，不引入新色。

- [ ] **Step 2: 把画板挂进画布**

`design/canvas.json` 的 `artboards` 数组加一条（`Article.dc.html` 之后）：

```json
{ "file": "Clips.dc.html", "x": 2400, "y": 0, "w": 1080, "h": 1600, "page": "page-1", "title": "剪藏栏目" }
```

- [ ] **Step 3: 提交**

```bash
git add design/Clips.dc.html design/canvas.json
git commit -m "视觉稿：剪藏栏目页"
```

---

### Task 4: 页面 `/clips` 与导航

**Files:**
- Create: `src/pages/clips.astro`
- Modify: `src/components/Header.astro`（加一条导航；顺带修手机端溢出）
- Modify: `src/styles/global.css`（末尾追加 `.clip-*` 一节）

**Interfaces:**
- Consumes: `src/data/clips.json`（Task 2 产出）
- Produces: `/Blog/clips` 路由

- [ ] **Step 1: 写页面**

`src/pages/clips.astro`：

```astro
---
import Base from '../layouts/Base.astro';
import clips from '../data/clips.json';

// clips.json 已经按剪藏时间倒序（pipeline/clips.py 的 collect 排好的），
// 这里顺序扫一遍就分好组，不再排第二次 —— 两处排序必然漂移。
const months: { month: string; items: typeof clips }[] = [];
for (const c of clips) {
  if (months.at(-1)?.month !== c.month) months.push({ month: c.month, items: [] });
  months.at(-1)!.items.push(c);
}

const SOURCE = { weixin: '公众号', x: 'X' } as const;
const facets = [
  { name: 'source', label: '来源', options: [['all', '全部'], ['weixin', '公众号'], ['x', 'X']] },
  { name: 'priority', label: '优先级', options: [['all', '全部'], ['高', '高'], ['中', '中'], ['低', '低']] },
];
---
<Base title="剪藏" description="读到的公众号文章与 X 帖子，附 AI 摘要、洞见与阅读优先级">
  <div class="wrap" style="padding-top: 48px; padding-bottom: 72px;">
    <h1 style="font-family: var(--serif); font-size: 26px; font-weight: 600; margin: 0 0 6px;">剪藏</h1>
    <p style="max-width: var(--measure); margin: 0 0 24px; color: var(--muted); font-size: 15px; line-height: 1.8;">
      平时读到的微信公众号文章与 X 帖子，经 AI 提炼成摘要、关键词、洞见与阅读优先级，
      收在 <a href="https://github.com/Bryce505/Notes">Bryce505/Notes</a>，本站原样索引。
      <strong>摘要与洞见由 AI 生成，不是作者本人的判断；正文不在本站</strong>——
      点标题去原文，点「全文快照」看剪藏当时的存档。
    </p>

    {clips.length === 0 ? (
      <p style="color: var(--muted);">还没有同步到剪藏。</p>
    ) : (
      <>
        {/* 没有 JS 时这一条整条不出现：筛选是便利，不是内容的前提，
            而一排点不动的按钮比没有按钮更糟。脚本末尾把它显出来。 */}
        <div class="clip-bar" data-clip-bar hidden>
          {facets.map((f) => (
            <div class="clip-facet" role="group" aria-label={`按${f.label}筛选`}>
              <span class="clip-facet-label">{f.label}</span>
              {f.options.map(([value, text], i) => (
                <button type="button" data-facet={f.name} data-value={value}
                        aria-pressed={i === 0 ? 'true' : 'false'}>{text}</button>
              ))}
            </div>
          ))}
          <span class="clip-count num" data-clip-count>共 {clips.length} 条</span>
        </div>

        {months.map((m) => (
          <section data-clip-group>
            <h2 class="clip-month">
              {m.month.replace('-', ' 年 ')} 月
              <span class="clip-month-n num">{m.items.length}</span>
            </h2>
            {m.items.map((c) => (
              <article class="clip" data-clip data-source={c.source} data-priority={c.priority}>
                <div class="clip-head">
                  <span class={`clip-tag clip-tag-${c.source}`}>{SOURCE[c.source as keyof typeof SOURCE]}</span>
                  <span class={`clip-prio clip-prio-${c.priority}`}>{c.priority}</span>
                  <h3 class="clip-title">
                    <a href={c.url} target="_blank" rel="noopener">{c.title}</a>
                  </h3>
                </div>
                <p class="clip-meta num">
                  <span>剪藏 {c.clippedAt}</span>
                  {c.publishedAt && <span>· 发布 {c.publishedAt}</span>}
                  {c.snapshot && <a href={c.snapshot} target="_blank" rel="noopener">· 全文快照 ↗</a>}
                </p>
                {c.keywords.length > 0 && (
                  <p class="clip-kw">{c.keywords.map((k) => <span>{k}</span>)}</p>
                )}
                {c.summary && <p class="clip-summary">{c.summary}</p>}
                {c.insights.length > 0 && (
                  <ul class="clip-insights">{c.insights.map((s) => <li>{s}</li>)}</ul>
                )}
                {c.verdict && <p class="clip-verdict">值不值得读：{c.verdict}</p>}
              </article>
            ))}
          </section>
        ))}
      </>
    )}
  </div>
</Base>

<script>
  // 纯前端筛选：条目全部渲染进 HTML，这里只切 hidden。数据量在两位数，
  // 分页或搜索都是过度设计。
  const bar = document.querySelector('[data-clip-bar]') as HTMLElement | null;
  if (bar) {
    const items = [...document.querySelectorAll<HTMLElement>('[data-clip]')];
    const groups = [...document.querySelectorAll<HTMLElement>('[data-clip-group]')];
    const count = document.querySelector('[data-clip-count]')!;
    const picked: Record<string, string> = { source: 'all', priority: 'all' };

    const apply = () => {
      let n = 0;
      for (const el of items) {
        const ok = Object.entries(picked).every(
          ([k, v]) => v === 'all' || el.dataset[k] === v);
        el.hidden = !ok;
        if (ok) n += 1;
      }
      // 整月都被筛掉时连月份标题一起收起来，否则会留下一排空标题
      for (const g of groups) g.hidden = !g.querySelector('[data-clip]:not([hidden])');
      count.textContent = `共 ${n} 条`;
    };

    bar.addEventListener('click', (e) => {
      const btn = (e.target as HTMLElement).closest<HTMLElement>('button[data-facet]');
      if (!btn) return;
      const facet = btn.dataset.facet!;
      picked[facet] = btn.dataset.value!;
      for (const b of bar.querySelectorAll(`button[data-facet="${facet}"]`))
        b.setAttribute('aria-pressed', String(b === btn));
      apply();
    });

    bar.hidden = false;
  }
</script>
```

- [ ] **Step 2: 加样式**

`src/styles/global.css` 末尾追加：

```css
/* ---- 剪藏栏目（src/pages/clips.astro）---- */
.clip-bar {
  display: flex; flex-wrap: wrap; align-items: center; gap: 10px 22px;
  padding: 12px 0 16px; margin-bottom: 8px;
  border-top: 1px solid var(--border); border-bottom: 1px solid var(--border);
}
.clip-facet { display: flex; flex-wrap: wrap; align-items: center; gap: 6px; }
.clip-facet-label { font-size: 13px; color: var(--muted); margin-right: 2px; }
.clip-bar button {
  font: inherit; font-size: 13px; line-height: 1.4; cursor: pointer;
  padding: 3px 11px; border-radius: 999px;
  border: 1px solid var(--border); background: transparent; color: var(--muted);
}
.clip-bar button:hover { color: var(--fg); border-color: var(--muted); }
.clip-bar button[aria-pressed='true'] {
  background: var(--accent); border-color: var(--accent); color: var(--bg);
}
.clip-count { margin-left: auto; font-size: 13px; color: var(--muted); }

.clip-month {
  font-family: var(--serif); font-size: 15px; font-weight: 600;
  letter-spacing: 0.04em; color: var(--muted);
  margin: 34px 0 0; padding-bottom: 8px;
}
.clip-month-n { font-size: 12px; margin-left: 8px; opacity: 0.75; }

.clip { padding: 20px 0; border-bottom: 1px solid var(--border); }
.clip-head { display: flex; flex-wrap: wrap; align-items: baseline; gap: 8px; }
.clip-title {
  font-family: var(--serif); font-size: 18px; font-weight: 600;
  line-height: 1.5; margin: 0; flex: 1 1 16rem;
}
.clip-title a { color: var(--fg); }

/* 徽标：来源用描边、优先级用实心，两者在同一行不至于糊成一片 */
.clip-tag, .clip-prio {
  font-size: 11.5px; line-height: 1.6; padding: 1px 8px; border-radius: 3px;
  white-space: nowrap; flex: none;
}
.clip-tag { border: 1px solid var(--border); color: var(--muted); }
.clip-tag-x { font-family: var(--mono); }
.clip-prio-高 { background: var(--accent); color: var(--bg); }
.clip-prio-中 { background: var(--surface); color: var(--muted); border: 1px solid var(--border); }
/* 低优先级本来就是「不必读」，做得比中更淡，扫的时候自然跳过 */
.clip-prio-低 { background: transparent; color: var(--muted); border: 1px dashed var(--border); opacity: 0.75; }

.clip-meta { display: flex; flex-wrap: wrap; gap: 6px; margin: 7px 0 0; font-size: 12.5px; color: var(--muted); }
.clip-kw { display: flex; flex-wrap: wrap; gap: 6px; margin: 10px 0 0; }
.clip-kw span {
  font-size: 12px; color: var(--muted);
  background: var(--surface); border-radius: 3px; padding: 1px 7px;
}
.clip-summary { max-width: var(--measure); margin: 11px 0 0; font-size: 14.5px; line-height: 1.8; }
.clip-insights {
  max-width: var(--measure); margin: 12px 0 0; padding: 2px 0 2px 15px;
  border-left: 2px solid var(--accent); list-style: none;
}
.clip-insights li { font-size: 14px; line-height: 1.75; color: var(--muted); }
.clip-insights li + li { margin-top: 5px; }
/* 「是否值得读」是判断不是内容，压到最小字号免得跟摘要抢注意力 */
.clip-verdict { max-width: var(--measure); margin: 11px 0 0; font-size: 12.5px; color: var(--muted); }

@media (max-width: 520px) {
  .clip-count { margin-left: 0; }
  .clip-title { font-size: 16.5px; }
}
```

- [ ] **Step 3: 改 Header**

`src/components/Header.astro` 的 `nav` 数组，在「文献周报」之后插一条（两者都是「收录别人的内容」，排一起）：

```js
  { href: `${base}/clips`, label: '剪藏' },
```

同一个文件顺带修手机端溢出（见 spec 第 7 节）。外层 div 的 `height: 68px` 换成可换行的写法：

```
style="display: flex; flex-wrap: wrap; align-items: baseline; gap: 32px; min-height: 68px; padding-top: 12px; padding-bottom: 12px;"
```

`nav` 的 style 加换行与行间距：

```
style="display: flex; flex-wrap: wrap; gap: 8px 22px; font-size: 14px; margin-left: auto;"
```

并在 nav 上方留一行注释说明为什么（实测数据 + 为什么不做汉堡菜单）。

- [ ] **Step 4: 构建**

Run: `npm run build 2>&1 | tail -20`
Expected: 构建成功，输出里有 `/clips/index.html`

- [ ] **Step 5: 核对产物**

```bash
grep -c 'class="clip "' dist/clips/index.html || grep -c 'data-clip=' dist/clips/index.html
grep -o 'data-priority="[^"]*"' dist/clips/index.html | sort | uniq -c
```

Expected: 68 条；优先级分布 51/10/7

- [ ] **Step 6: 提交**

```bash
git add src/pages/clips.astro src/components/Header.astro src/styles/global.css
git commit -m "剪藏栏目页 /clips，导航加一条并修手机端溢出"
```

---

### Task 5: 同步 workflow

**Files:**
- Create: `.github/workflows/sync-clips.yml`
- Modify: `.github/workflows/deploy.yml`（`workflow_run.workflows` 加 `sync-clips`）

**Interfaces:**
- Consumes: `pipeline/clips.py` 的命令行接口（`--notes` / `--out`）
- Produces: 定时与手动触发的 `src/data/clips.json` 更新提交

- [ ] **Step 1: 写 sync-clips.yml**

```yaml
# 把 Bryce505/Notes 的月度剪藏索引解析进 src/data/clips.json。
# 只取索引不取全文快照：公众号正文是别人的版权内容，且 archive/ 一个月几百 KB。
name: sync-clips

on:
  schedule:
    # 北京时间次日 06:23 取前一天的剪藏。刻意避开整点：本仓库实测整点排队会
    # 让 schedule 整天不触发，见 docs/devlog/2026-08-27-定时任务整点排队延迟.md
    - cron: '23 22 * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: sync-clips
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Notes 是公开仓库，内置 GITHUB_TOKEN 就能读，不需要 PAT
      - uses: actions/checkout@v4
        with:
          repository: Bryce505/Notes
          path: .clips

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # clips.py 零第三方依赖，不装 pipeline/requirements.txt
      - name: 解析剪藏索引
        run: python pipeline/clips.py --notes .clips --out src/data/clips.json

      - name: 提交
        run: |
          rm -rf .clips
          git config user.name  'blog-bot'
          git config user.email 'makejun505@gmail.com'
          git add src/data/clips.json
          if git diff --staged --quiet; then
            echo '剪藏没有更新'
          else
            git commit -m "同步剪藏：$(date -u +%Y-%m-%d)"
            git push
          fi
```

**刻意不加备份触发**（`sync-reports.yml` 有两条 cron）：周报一周一期，漏一次就是站上少一整周；剪藏每天跑且同步幂等，漏一天第二天连着补上，没有窗口丢失。这条理由写进 workflow 注释。

- [ ] **Step 2: 把 sync-clips 挂进 deploy 的触发链**

`.github/workflows/deploy.yml` 里：

```yaml
  workflow_run:
    workflows: [publish, sync-reports, sync-clips]
    types: [completed]
```

并把上面那段已有注释的最后一句改成同时点名两个同步 workflow：

```
  # sync-reports 与 sync-clips 同理：它们推的周报与剪藏也得靠这条才上线。
```

- [ ] **Step 3: 校验 YAML 能被解析**

```bash
python3 -c "
import sys, json
try: import yaml
except ImportError: sys.exit('装 pyyaml 才能校验，跳过：pip install pyyaml')
for f in ('.github/workflows/sync-clips.yml', '.github/workflows/deploy.yml'):
    d = yaml.safe_load(open(f))
    print(f, '->', list(d['on']))
"
```

Expected: `sync-clips.yml -> ['schedule', 'workflow_dispatch']`；`deploy.yml` 的 `workflow_run.workflows` 含三个

- [ ] **Step 4: 本地复跑一次 workflow 的核心命令，确认幂等**

```bash
.venv/bin/python pipeline/clips.py --notes /tmp/notes-upstream --out src/data/clips.json
git diff --stat src/data/clips.json
```

Expected: 无 diff —— 同一份上游跑两次产出必须逐字节一致，否则每天都会推一条假更新

- [ ] **Step 5: 提交**

```bash
git add .github/workflows/sync-clips.yml .github/workflows/deploy.yml
git commit -m "sync-clips workflow：每天把 Notes 的剪藏索引同步进来"
```

---

### Task 6: README 与开发日志

**Files:**
- Modify: `README.md`（目录、架构总览图、workflow 表、仓库目录结构、日常使用新增一节）
- Create: `docs/devlog/2026-09-05-剪藏栏目.md`
- Modify: `docs/devlog/README.md`（目录最上面加一行）

**Interfaces:**
- Consumes: 前五个任务的全部产出
- Produces: 无（文档任务）

- [ ] **Step 1: README 改五处**

1. **目录**：`## 目录` 列表里在「日常使用」后面不动，本节挂在「日常使用」下面，无需新增顶级条目
2. **架构总览的 ASCII 图**：在「另一条通道（文献周报…）」之后追加第三条通道的图，标明不经过 pipeline 的五步流程也不经过校验器
3. **四个 workflow 表**改成五个，加一行 `sync-clips`
4. **仓库目录结构**：`pipeline/` 下加 `clips.py`，`src/` 下加 `data/clips.json` 与 `pages/clips.astro`，`design/` 说明不变
5. **日常使用**加一节「剪藏是怎么上站的」，照「文献周报是怎么上站的」那一节的结构写：数据来源、为什么只取索引不取全文、为什么解析器在 Python、**手动触发怎么点（含手机端）**、怎么验证这条通道还活着

手动触发那段必须写清：Actions → sync-clips → Run workflow，分支 `master`，无参数；跑完看日志是不是「剪藏没有更新」（上游没新剪藏时这就是正确结果，它证明「解析 → diff 判定」链路通且幂等）；再看 Actions 列表里有没有紧跟着一条 `deploy`。安卓 GitHub App 的已知毛病（从没手动跑过的 workflow 不显示按钮）照抄 `sync-reports` 那节的绕法。

- [ ] **Step 2: 写开发日志**

`docs/devlog/2026-09-05-剪藏栏目.md`，按 `docs/devlog/README.md` 的模板六节：日期 / 背景 / 变更 / 实现 / 动了哪些文件 / 验证。

「实现」一节要写清三个取舍与放弃的方案：为什么解析器在 Python 不在 Astro、为什么 `clips.json` 不做成 content collection、为什么不镜像全文快照。「变更」一节**单独注明**顺带修的导航溢出（`CLAUDE.md` 要求无关 bug 单独标注）。「验证」写实测数字：自检项数、真实数据条数与分布、构建产物核对结果。

- [ ] **Step 3: 目录加一行**

`docs/devlog/README.md` 的「目录」列表最上面插入：

```markdown
- [2026-09-05　剪藏栏目：公众号文章与 X 帖子上站，顺带修导航手机端溢出](2026-09-05-剪藏栏目.md)
```

- [ ] **Step 4: 全量复验**

```bash
.venv/bin/python pipeline/test_pipeline.py 2>&1 | tail -3
npm run build 2>&1 | tail -5
```

Expected: 自检 `162/162 passed`（数字以实际输出为准）；构建成功

- [ ] **Step 5: 提交**

```bash
git add README.md docs/devlog/2026-09-05-剪藏栏目.md docs/devlog/README.md
git commit -m "README 与开发日志：剪藏栏目"
```

---

## 自检（本计划对照 spec）

| spec 小节 | 对应任务 |
|---|---|
| 2 决策记录 · 只读仓库不接 Notion | Task 5（workflow 只 checkout 一个仓库） |
| 2 决策记录 · 不镜像全文快照 | Task 1（`_snapshot_url` 只生成链接） |
| 2 决策记录 · 单页合并流 + 筛选 | Task 4 |
| 2 决策记录 · 独立栏目不进 RSS/标签/归档 | 全计划都不碰那几个文件（Global Constraints 已列） |
| 3 解析器在 Python + 不做 collection | Task 1、Task 4 Step 1（直接 import） |
| 4 数据流 · deploy 的 workflow_run | Task 5 Step 2 |
| 5 字段来源与缺失处理 | Task 1 Step 2 的 `test_clips_parses_every_field` / `test_clips_optional_fields_may_be_absent` / `test_clips_missing_required_field_raises` |
| 5 priority 三档 | `test_clips_priority_outside_three_buckets_raises` |
| 5 零条目抛错 | `test_clips_zero_entries_raises`、`test_clips_collect_without_any_month_file_raises` |
| 5 快照路径还原 + 不二次编码 | `test_clips_snapshot_relative_path_becomes_repo_url`、`test_clips_snapshot_path_is_not_encoded_twice` |
| 6 页面设计（页头/工具条/卡片/分组/空态） | Task 4 Step 1、Step 2 |
| 7 导航手机端溢出 | Task 4 Step 3 |
| 8 验证方式 | Task 1 Step 5、Task 2、Task 4 Step 4-5、Task 5 Step 4 |
