# 站内系列栏目 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让一组文章能以指定顺序成为「系列」，有独立落地页与简介，读者在任一篇里都能看到自己身处系列何处、上下篇是什么。

**Architecture:** 系列的归属与顺序全部存在 `src/content/series.json` 一个文件里，用 Astro 原生 `file()` loader 读成 content collection。解析逻辑集中在 `src/lib/series.ts` 一处，三个页面共用。文章的 frontmatter 与 `pipeline/` 完全不动。

**Tech Stack:** Astro 7.2.6（`astro/loaders` 的 `file()`）、zod（Astro 内置）、TypeScript

**Spec:** `docs/superpowers/specs/2026-08-26-系列栏目-design.md`

## Global Constraints

- **不碰 `pipeline/` 任何文件**，不改 posts 的 frontmatter schema
- 站点无 JS 测试框架，本功能的自检就是 `npm run build`：两项硬校验都设计成**构建期抛错**，所以「构建能否通过」本身即测试
- 注释写「为什么」不写「做了什么」（`CLAUDE.md` 约定）
- 中文注释与文档，术语保留英文原词
- `file()` 的路径相对**项目根**解析（`new URL(fileName, config.root)`），写 `src/content/series.json`
- CSS 只用已有令牌：`--serif` `--muted` `--border` `--fg` `--measure`，工具类 `.wrap` `.num` `.eyebrow`
- 一个分支一篇开发日志（`CLAUDE.md` 约定），最后一个任务统一补

---

### Task 1: 数据层与解析逻辑

**Files:**
- Create: `src/content/series.json`
- Create: `src/lib/series.ts`
- Modify: `src/content.config.ts`

**Interfaces:**
- Consumes: 无（首个任务）
- Produces: `loadSeries(): Promise<ResolvedSeries[]>`、`seriesContextOf(postId: string): Promise<SeriesContext | null>`；类型 `SeriesItem { post, title, no }`、`ResolvedSeries { id, title, description, items, publishedCount }`、`SeriesContext { series, no, total, prev, next }`

- [ ] **Step 1: 写 series.json，用真实数据起一个系列**

站上已发布的技术文章只有 `00基础-生物制品-单抗` 一篇，其余同主题的稿子都还在 `_review/` 等人工放行 —— 正好用 `planned` 表达。

```json
[
  {
    "id": "单抗表征方法",
    "title": "单抗表征方法",
    "description": "从单抗的结构基础与质量属性出发，沿「整体表征 → 高阶结构 → 杂质与残留 → 可开发性」的线索展开。计划中的篇目会随 _review/ 里的稿子放行而陆续补上。",
    "entries": [
      { "post": "00基础-生物制品-单抗" },
      { "planned": "光散射技术表征生物药：SLS/DLS 的原理与应用" },
      { "planned": "宿主细胞蛋白（HCP）的质谱鉴定与绝对定量" },
      { "planned": "治疗蛋白候选物的可开发性评估" },
      { "planned": "结构表征研究策略：从早研到 BLA" }
    ]
  }
]
```

- [ ] **Step 2: 在 content.config.ts 里声明 series collection**

在文件顶部把 import 改成同时引入 `file`：

```ts
import { file, glob } from 'astro/loaders';
```

在 `posts` 定义之后、`export const collections` 之前插入：

```ts
// 系列的归属与顺序只存在这一个文件里：写进每篇文章的 frontmatter 的话，
// 调一次顺序要改 N 个文件，且两篇写了同一个序号没有任何东西会拦。
const series = defineCollection({
  loader: file('src/content/series.json'),
  schema: z.object({
    title: z.string(),
    description: z.string().default(''),
    // 每条 entry 恰好有 post 或 planned 之一。两者都写或都不写一律构建失败——
    // 手写 JSON 最容易把文章 id 打错，而打错的 id 绝不能静默退化成
    // 一条看不出错的「待发布」幽灵条目。
    entries: z.array(
      z.object({
        post: z.string().optional(),
        planned: z.string().optional(),
      }).refine((e) => !!e.post !== !!e.planned, {
        message: 'entry 必须恰好有 post 或 planned 之一',
      }),
    ).default([]),
  }),
});
```

把最后一行改成：

```ts
export const collections = { posts, series };
```

- [ ] **Step 3: 写 src/lib/series.ts**

```ts
import { getCollection } from 'astro:content';

export interface SeriesItem {
  /** 已发布的文章条目；待发布条目为 null */
  post: any | null;
  title: string;
  /** 从 1 起的序号，含待发布条目 */
  no: number;
}

export interface ResolvedSeries {
  id: string;
  title: string;
  description: string;
  items: SeriesItem[];
  publishedCount: number;
}

export interface SeriesContext {
  series: ResolvedSeries;
  no: number;
  total: number;
  prev: SeriesItem | undefined;
  next: SeriesItem | undefined;
}

/** 把 series.json 的 entries 解析成文章对象，并做两项构建期硬校验。 */
export async function loadSeries(): Promise<ResolvedSeries[]> {
  const [seriesEntries, posts] = await Promise.all([
    getCollection('series'),
    getCollection('posts'),
  ]);
  const byId = new Map(posts.map((p) => [p.id, p]));
  // 文章 id -> 已归属的系列 id，用于拦「一篇进了两个系列」
  const owner = new Map<string, string>();

  return seriesEntries.map((s) => {
    const items: SeriesItem[] = s.data.entries.map((e: any, i: number) => {
      const no = i + 1;
      if (!e.post) return { post: null, title: e.planned, no };

      const post = byId.get(e.post);
      // 打错的 id 必须让构建炸掉。若容许「找不到就当作待发布」，
      // 一个拼写错误会变成页面上一条看不出错的灰条目。
      if (!post) {
        throw new Error(
          `series.json：系列「${s.id}」的第 ${no} 条引用了不存在的文章 id「${e.post}」`,
        );
      }
      // 文章页的系列归属提示只有一处，属于两个系列时显示哪个都是错的。
      const prev = owner.get(e.post);
      if (prev) {
        throw new Error(
          `series.json：文章「${e.post}」同时属于系列「${prev}」和「${s.id}」`,
        );
      }
      owner.set(e.post, s.id);
      return { post, title: post.data.title, no };
    });

    return {
      id: s.id,
      title: s.data.title,
      description: s.data.description,
      items,
      publishedCount: items.filter((i) => i.post).length,
    };
  });
}

/** 找出某篇文章所属的系列及其上下篇；不属于任何系列返回 null。 */
export async function seriesContextOf(postId: string): Promise<SeriesContext | null> {
  for (const s of await loadSeries()) {
    const idx = s.items.findIndex((i) => i.post?.id === postId);
    if (idx === -1) continue;
    // 上下篇跳过待发布条目：它们没有页面可链
    const prev = [...s.items.slice(0, idx)].reverse().find((i) => i.post);
    const next = s.items.slice(idx + 1).find((i) => i.post);
    return { series: s, no: s.items[idx].no, total: s.items.length, prev, next };
  }
  return null;
}
```

- [ ] **Step 4: 构建，确认数据层不报错**

Run: `npm run build`
Expected: 构建成功。此时还没有页面消费 series，本步只验证 collection 与 schema 能被加载。

- [ ] **Step 5: 故意打错 id，确认构建失败（这是本功能的核心自检）**

```bash
sed -i 's/"post": "00基础-生物制品-单抗"/"post": "00基础-生物制品-单抗-打错了"/' src/content/series.json
npm run build 2>&1 | grep -c "不存在的文章 id"
```

Expected: 构建**失败**，且输出里能 grep 到「不存在的文章 id」。

注意：Task 1 结束时还没有页面调用 `loadSeries()`，所以这一步要等 Task 2 的页面写完才真正生效。**把本步连同 Step 6 一起挪到 Task 2 末尾执行**，此处保留说明是为了让实现者知道这两项校验必须被验证过。

- [ ] **Step 6: 改回正确的 id**

```bash
git checkout src/content/series.json
```

- [ ] **Step 7: 提交**

```bash
git add src/content/series.json src/lib/series.ts src/content.config.ts
git commit -m "系列栏目：数据层与解析逻辑"
```

---

### Task 2: 系列索引页与系列详情页

**Files:**
- Create: `src/pages/series/index.astro`
- Create: `src/pages/series/[series].astro`
- Modify: `src/components/Header.astro`

**Interfaces:**
- Consumes: `loadSeries()`、`ResolvedSeries`、`SeriesItem`（Task 1）
- Produces: 路由 `/series` 与 `/series/<id>`

- [ ] **Step 1: 写系列索引页**

`src/pages/series/index.astro`：

```astro
---
import Base from '../../layouts/Base.astro';
import { loadSeries } from '../../lib/series';

const base = import.meta.env.BASE_URL.replace(/\/$/, '');
const all = await loadSeries();
---
<Base title="系列" description="按顺序成组的文章">
  <div class="wrap" style="padding-top: 48px; padding-bottom: 72px;">
    <h1 style="font-family: var(--serif); font-size: 26px; font-weight: 600; margin: 0 0 6px;">系列</h1>
    <p style="margin: 0 0 28px; color: var(--muted); font-size: 14px; max-width: var(--measure); line-height: 1.8;">
      同一主题下按顺序成组的文章。标为「待发布」的是已经规划、还没写完的篇目。
    </p>

    {all.length === 0 && (
      <p style="color: var(--muted);">还没有系列。在 <code>src/content/series.json</code> 里加一条即可。</p>
    )}

    {all.map((s) => (
      <article style="padding: 22px 0; border-bottom: 1px solid var(--border);">
        <h2 style="margin: 0 0 7px; font-family: var(--serif); font-size: 20px; font-weight: 600;">
          <a href={`${base}/series/${encodeURIComponent(s.id)}`} style="color: var(--fg);">{s.title}</a>
        </h2>
        <p style="margin: 0 0 8px; max-width: var(--measure); font-size: 15px; color: var(--muted); line-height: 1.8; text-wrap: pretty;">
          {s.description}
        </p>
        <p style="margin: 0; font-size: 12.5px; color: var(--muted);">
          已发布 <span class="num">{s.publishedCount}</span> / 共 <span class="num">{s.items.length}</span> 篇
        </p>
      </article>
    ))}
  </div>
</Base>
```

- [ ] **Step 2: 写系列详情页**

`src/pages/series/[series].astro`。用编号目录而不是文章卡片流：卡片流表达不出顺序，也放不下「待发布」这种没有链接的条目。

```astro
---
import Base from '../../layouts/Base.astro';
import { loadSeries } from '../../lib/series';

export async function getStaticPaths() {
  const all = await loadSeries();
  return all.map((s) => ({ params: { series: s.id }, props: { s } }));
}

const { s } = Astro.props;
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
const fmt = (d: Date) =>
  `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, '0')}-${String(d.getDate()).padStart(2, '0')}`;
---
<Base title={s.title} description={s.description}>
  <div class="wrap" style="padding-top: 48px; padding-bottom: 72px;">
    <p style="margin: 0 0 8px; font-size: 13px; color: var(--muted);">
      <a href={`${base}/series`} style="color: var(--muted);">系列</a>
    </p>
    <h1 style="font-family: var(--serif); font-size: 26px; font-weight: 600; margin: 0 0 10px;">{s.title}</h1>
    <p style="margin: 0 0 6px; max-width: var(--measure); font-size: 15.5px; color: var(--muted); line-height: 1.85; text-wrap: pretty;">
      {s.description}
    </p>
    <p style="margin: 0 0 28px; font-size: 13px; color: var(--muted);">
      已发布 <span class="num">{s.publishedCount}</span> / 共 <span class="num">{s.items.length}</span> 篇
    </p>

    <ol style="list-style: none; margin: 0; padding: 0;">
      {s.items.map((i) => (
        <li style="display: flex; gap: 14px; align-items: baseline; padding: 13px 0; border-bottom: 1px solid var(--border);">
          <span class="num" style="color: var(--muted); font-size: 13px; min-width: 24px;">{i.no}</span>
          <div style="min-width: 0;">
            {i.post ? (
              <>
                <a href={`${base}/posts/${i.post.id}`} style="color: var(--fg); font-size: 16px;">{i.title}</a>
                {i.post.data.description && (
                  <p style="margin: 5px 0 0; max-width: var(--measure); font-size: 14px; color: var(--muted); line-height: 1.75; text-wrap: pretty;">
                    {i.post.data.description}
                  </p>
                )}
              </>
            ) : (
              <span style="color: var(--muted); font-size: 16px;">{i.title}</span>
            )}
          </div>
          <span style="margin-left: auto; font-size: 12px; color: var(--muted); white-space: nowrap;">
            {i.post ? <time class="num" datetime={fmt(i.post.data.date)}>{fmt(i.post.data.date)}</time> : '待发布'}
          </span>
        </li>
      ))}
    </ol>
  </div>
</Base>
```

- [ ] **Step 3: 主导航加「系列」**

`src/components/Header.astro` 的 `nav` 数组，在「归档」之前插入一行：

```ts
  { href: `${base}/series`, label: '系列' },
```

- [ ] **Step 4: 构建并确认两个路由都生成了**

Run: `npm run build && ls dist/series && ls dist/series/*/index.html`
Expected: 构建成功，`dist/series/index.html` 与 `dist/series/单抗表征方法/index.html` 都存在。

- [ ] **Step 5: 跑 Task 1 Step 5 的错拼校验（此时才真正生效）**

```bash
sed -i 's/"post": "00基础-生物制品-单抗"/"post": "打错的id"/' src/content/series.json
npm run build 2>&1 | grep "不存在的文章 id" && echo "校验生效"
git checkout src/content/series.json
```

Expected: 构建失败，且能 grep 到「不存在的文章 id」。

- [ ] **Step 6: 跑「一篇进两个系列」校验**

```bash
python3 - <<'PY'
import json, pathlib
p = pathlib.Path('src/content/series.json')
d = json.loads(p.read_text(encoding='utf-8'))
d.append({"id": "重复测试", "title": "重复测试", "description": "",
          "entries": [{"post": "00基础-生物制品-单抗"}]})
p.write_text(json.dumps(d, ensure_ascii=False, indent=2), encoding='utf-8')
PY
npm run build 2>&1 | grep "同时属于系列" && echo "校验生效"
git checkout src/content/series.json
```

Expected: 构建失败，且能 grep 到「同时属于系列」。

- [ ] **Step 7: 提交**

```bash
git add src/pages/series src/components/Header.astro
git commit -m "系列栏目：索引页、详情页与导航入口"
```

---

### Task 3: 文章页的系列上下文与上下篇导航

**Files:**
- Modify: `src/pages/posts/[...slug].astro`
- Modify: `src/layouts/Post.astro`

**Interfaces:**
- Consumes: `seriesContextOf(postId)`、`SeriesContext`（Task 1）
- Produces: 无（终端消费者）

- [ ] **Step 1: 在文章页算好系列上下文传下去**

`src/pages/posts/[...slug].astro`，在 import 区加：

```ts
import { seriesContextOf } from '../../lib/series';
```

在 `related` 计算之后加：

```ts
const series = await seriesContextOf(entry.id);
```

把渲染那行改成：

```astro
<Post entry={entry} headings={headings} related={related} series={series}>
```

- [ ] **Step 2: Post.astro 接收 series 并显示归属**

把 Props 与解构改成：

```ts
interface Props { entry: any; headings: any[]; related: any[]; series?: any; }
const { entry, headings, related, series } = Astro.props;
```

在标题下方那个 meta 行（含 `<time>` 与「由 N 篇笔记整理」的 div）里，`{count > 0 && (...)}` 之后追加：

```astro
        {series && (
          <><span aria-hidden="true" style="color: var(--border);">·</span>
          <span>
            <a href={`${base}/series/${encodeURIComponent(series.series.id)}`} style="color: var(--muted);">{series.series.title}</a>
            　第 <span class="num">{series.no}</span> / <span class="num">{series.total}</span> 篇
          </span></>
        )}
```

- [ ] **Step 3: Post.astro 底部加上下篇导航**

在「相关文章」那个 `{related.length > 0 && (...)}` 段**之前**插入：

```astro
        {series && (series.prev || series.next) && (
          <section>
            <h2 class="eyebrow">本系列</h2>
            <div style="display: grid; gap: 10px;">
              {series.prev && (
                <a href={`${base}/posts/${series.prev.post.id}`} style="font-size: 14.5px;">
                  ← 上一篇　{series.prev.title}
                </a>
              )}
              {series.next && (
                <a href={`${base}/posts/${series.next.post.id}`} style="font-size: 14.5px;">
                  下一篇 →　{series.next.title}
                </a>
              )}
            </div>
          </section>
        )}
```

- [ ] **Step 4: 构建并确认系列文章页带上了归属**

Run: `npm run build && grep -c "第 <span class=\"num\">1</span>" "dist/posts/00基础-生物制品-单抗/index.html"`
Expected: 构建成功，grep 结果 ≥ 1。

- [ ] **Step 5: 确认不属于任何系列的文章页没受影响**

Run: `grep -c "本系列" dist/posts/tools-笔记/index.html || echo "0（正确：不属于系列的文章不显示系列块）"`
Expected: 0。

- [ ] **Step 6: 提交**

```bash
git add src/pages/posts src/layouts/Post.astro
git commit -m "系列栏目：文章页的系列归属与上下篇导航"
```

---

### Task 4: 浏览器实测、文档与开发日志

**Files:**
- Create: `docs/devlog/2026-08-26-系列栏目.md`
- Modify: `docs/devlog/README.md`
- Modify: `README.md`

**Interfaces:**
- Consumes: 前三个任务的全部产出
- Produces: 无

- [ ] **Step 1: 确认流水线自检没被误伤**

Run: `python3 pipeline/test_pipeline.py 2>&1 | tail -3`
Expected: `117/117 passed`。本次不动 `pipeline/`，此项用于确认没有误伤。

- [ ] **Step 2: Chromium 逐页实测**

```bash
npm run preview &
```

用 Playwright（`PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers` 已配好）逐页截图核对：

- `/Blog/series` — 索引页，系列标题、简介、「已发布 1 / 共 5 篇」
- `/Blog/series/单抗表征方法` — 编号目录，第 1 条可点，第 2~5 条灰且标「待发布」
- `/Blog/posts/00基础-生物制品-单抗` — 标题下有系列归属「单抗表征方法　第 1 / 5 篇」
- 深色主题（`data-theme="dark"`）下同样三页

- [ ] **Step 3: README 补一节「怎么建一个系列」**

在「日常使用」章节下加：

````markdown
### 怎么建一个系列

编辑 `src/content/series.json`，加一条：

```json
{
  "id": "系列的URL名",
  "title": "系列标题",
  "description": "一句话说明这个系列讲什么、按什么线索展开。",
  "entries": [
    { "post": "已发布文章的文件名（不含 .md）" },
    { "planned": "还没写的篇目标题" }
  ]
}
```

`entries` 的数组顺序**就是**阅读顺序，不用另写序号。`planned` 条目在页面上显示为
「待发布」，写完后把它换成 `{ "post": "..." }` 即可。

两条硬规矩，违反会让构建直接失败（不是静默出错）：

- `post` 填的 id 必须真实存在 —— 打错字不会变成一条看不出错的灰条目
- 一篇文章只能属于一个系列 —— 文章页的归属提示只有一处
````

- [ ] **Step 4: 写开发日志**

`docs/devlog/2026-08-26-系列栏目.md`，按 `docs/devlog/README.md` 的模板写齐六个字段：日期、背景、变更、实现、动了哪些文件、验证。

背景要写实测现象：分类与标签都是无序集合，知识库里成串的内容（ELISA 的 MRD → 稀释线性 → 平行性）顺序信息完全丢失。

实现里要交代取舍：为什么系列数据放单文件而不是 frontmatter、为什么错拼 id 要炸构建、为什么上下篇跳过待发布条目。

另起一节说明本分支还顺带装了 `.claude/`（superpowers + ponytail 技能包），那是工具链不是本功能。

在 `docs/devlog/README.md` 的目录最上面加一行。

- [ ] **Step 5: 提交并推送**

```bash
git add docs README.md
git commit -m "系列栏目：开发日志与使用说明"
git push -u origin claude/topic-series-publishing-plan-meauda
```

- [ ] **Step 6: 开 PR**

标题「站内系列栏目」，正文说明：数据放哪、两条硬校验、不碰流水线、验证结果。
