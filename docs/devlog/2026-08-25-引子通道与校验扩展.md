# 2026-08-25　引子通道（加减法）、校验器扩展、人工投稿通道

## 背景

自动通道是「多篇笔记打散重组」，产出拼接感很强：几篇笔记各讲各的，模型
只能按顺序摆，覆盖面广但每节浅尝辄止 —— 实测那篇 8 篇笔记合成的《治疗性
单抗质量属性与表征方法》就是这个毛病，读起来像目录不像文章。

先试了「单篇笔记做引子让模型扩写」，三篇样稿结构明显好转（成药性那篇
4,043 → 7,724 字符，补出的「常见误区与边界条件」一整节原笔记完全没有，
且纯定性、零编造数字）。据此确定取材方式改为引子通道，并按笔记完整度
分加减法两种模式。

新的风险是模型可能编出处。「有没有幻觉」没法用正则判定，得换个形式解决。

## 变更

- 新增：`pipeline/evidence.py` 解析并核验「依据与出处」标注
- 新增：`pipeline/manual.py` 人工投稿通道（drafts/ 稿件走同一套加减法与校验）
- 新增：`pipeline/notify.py` 校验未通过时写日志、开 issue、发邮件
- 新增：`verify.review()` 六组可发表性检查（结构 / 格式 / 加减法比例 /
  选材重复 / 关联文章链接 / 扩展来源核验）
- 新增：`images.copy_local()` 随稿上传的本地图片就地转 WebP
- 新增：`render.append_related()` 确定性生成「相关阅读」链接
- 修改：`prompt_seed.md` 改为加减法原则 + 强制来源标注格式
- 修改：`seed.py` 拆出可复用的 `process()`；按体量决定加减法；加法模式
  把同主题的相关笔记片段一起喂给模型当扩展素材
- 修改：`publish.yml` 定时改跑引子通道，新增 mode / publish 两个手动开关

## 实现

**把不可判定的问题转成可判定的。** 要求模型为每处扩展写 `[依据 N]` 标注，
并在文末列出可核验的来源（知识库路径或 DOI）。机器逐条回原文核对：路径
必须真实存在、引用要点必须在那篇笔记里找得到、DOI 必须在输入材料里出现过。
编造的出处在这三处必然露馅。

引用要点用**字符二元组重合度**（阈值 0.5）而不是全句匹配：模型转述是正常
的，逐字比对会把所有正常转述判成幻觉。中文没有空格分词，二元组是不引入
分词依赖的最省事代理。阈值刻意宽松 —— 这个检查要拦的是整条编造，不是做
相似度评分。

**加减法用篇幅比例做确定性判定**：减法 55%～95%，加法 105%～300%。缩到
一半以下说明删掉了不该删的，翻三倍以上是过度扩展。

**扩展素材优先从知识库取**：加法模式下把同三级标签的相关笔记（最多 6 篇、
每篇截 3000 字符）随提示词一起喂进去，模型只能引用这些片段里出现过的路径。
这样「来源」天然可核验，比让它凭记忆写文献可靠得多。

**关联文章的链接由流水线生成**，不让模型写：模型不知道站上已经发过什么，
让它写必然编 slug。

**通知三条出口互相独立**：日志随产出一起提交（翻历史不用翻 Actions 页面），
GitHub Issue 零配置且手机有推送，SMTP 配了才发。通知失败绝不能让流水线
失败 —— 文章已经落盘了，为了发不出去的通知把产出丢掉是本末倒置。

## 动了哪些文件

| 文件 | 动作 | 说明 |
|---|---|---|
| `CLAUDE.md` | 新增 | 开发日志规矩与代码约定 |
| `docs/devlog/` | 新增 | 开发日志目录与模板 |
| `pipeline/evidence.py` | 新增 | 来源标注解析与核验 |
| `pipeline/manual.py` | 新增 | 人工投稿通道 |
| `pipeline/notify.py` | 新增 | 日志 / issue / 邮件 |
| `pipeline/prompt_seed.md` | 修改 | 加减法原则 + 来源标注格式 |
| `pipeline/seed.py` | 修改 | 拆出 `process()`；加减法；相关片段检索 |
| `pipeline/verify.py` | 修改 | 新增 `review()` 六组检查 |
| `pipeline/images.py` | 修改 | `copy_local()` |
| `pipeline/render.py` | 修改 | `append_related()` |
| `pipeline/main.py` | 修改 | `--seed` / `--manual` / `--publish`；收尾发通知 |
| `.github/workflows/publish.yml` | 修改 | mode 开关、issues 权限、日志一起提交 |
| `README.md` | 修改 | 五条通道、人工投稿流程、校验清单、通知配置 |

## 验证

自检 108 → 115 项，新增的七项覆盖：加减法模式判定、减法删过头被拦、加法
没写依据被拦、编造出处被拦、转述式引用要点放行、关联文章自动带链接、
随稿图片就地转换且原稿撤走。

引子通道的三篇实跑样稿在 `_review/` 下（HCP 鉴定与定量、光散射表征、
成药性评估），其中 HCP 那篇被校验器拦下 3 个模型自己补的数值 —— 红线
按预期生效。


## 实测修正（2026-08-26）

分支上实跑六篇，改了三处标定与一个真 bug：

1. **加法比例上限配绝对下限。** 一篇 201 字符的人工投稿，3 倍上限只有
   600 字符，成不了文章。比例是拦「跑飞」的，不是惩罚「引子短」的 ——
   上限改成 `max(原文×3, 4000 字符)`。
2. **自动选材限定 2k~40k。** 原本按字数降序排，最长的先上，等于每次都
   挑最难的：105k 那篇被压到 13%、丢 10 个数值；63.6k 那篇比例合格但丢
   1 图 2 DOI。10 万字符的笔记要求输出 6 万字，超出模型输出上限，这不是
   模型不听话，是要求本身不现实。区间外仍可用 `--seed` 显式指定。
3. **相关片段里的数字不算编造（真 bug）。** 加法模式把片段喂给模型并
   要求优先取材，保真校验却只拿引子笔记比对 —— 模型老实引用片段里的
   50 pmol 反被判成幻觉，那篇 label-free 定量因此被拦下 10 个数，其中
   还有论文年份（在标题里不在正文里）。`verify()` 新增 `extra_src`：
   只参与「有没有凭空多出数据」的比对，不参与篇幅比例，否则喂几篇片段
   就能把「正文过短」稀释掉。
4. `sample.yml` 的提交列表补上 `logs/`。

### 六篇实跑的结果

| 引子 | 模式 | 篇幅 | 结果 |
|---|---|---|---|
| ELISA方法开发-稀释线性和平行性 | 加法 | 7,686 → 10,469 | **全过**。6 章节、10 处依据标注、3 条来源全部通过核对 |
| 分项策略-结构表征研究策略 | 减法 | 37,165 → 26,130（70%） | 结构格式图片比例全过，丢 2/5 DOI |
| 切胶覆盖率 | 加法 | 7,306 → 5,222 | 加法模式却缩了，且编了 5 个数 —— 按预期拦下 |
| 测试稿-CE-SDS进样重现性 | 加法 | 201 → 3,314 | 编 6 个数、扩 1661%、2 条引用要点核不上 —— 三道闸全响 |
| QC检测流程梳理 | 减法 | 63,602 → 42,062 | 丢 1 图、丢 2/3 DOI |
| 重组尿酸氧化酶活性分析方法开发 | 减法 | 105,190 → 13,945 | 压到 13% —— 促成了第 2 条修正 |

人工投稿通道的完整链路同时验证：随稿图片就地转 WebP、原稿处理后撤走、
落 `_review/` 未发布、`logs/verify-2026-08.md` 写入、自动开 issue #1。
