# 本仓库的开发约定

## 开发日志（硬性要求）

**每一次改动都必须留开发日志。** 日志是这个项目唯一的连续记忆：流水线的
每条规则背后都有一次实测踩坑，不记下来，半年后没人知道那行代码为什么长
成那样，改回去就再踩一遍。

### 怎么写

一次改动一个文件，放在 `docs/devlog/`，文件名 `YYYY-MM-DD-<短标题>.md`：

```
docs/devlog/2026-08-25-引子通道.md
```

**一次改动一个文件，不要往同一个文件里追加。** 这是为了合并：多个分支
同时改一个 CHANGELOG 必然冲突，一次改动一个文件则天然无冲突，文件名排序
就是时间顺序。PR 合并时按文件名先后排列即可，不需要手工整理。

### 每篇必须包含

| 字段 | 说明 |
|---|---|
| 日期 | 文件名里的日期，正文首行再写一次 |
| 背景 | 为什么要改。**写实测现象，不写「优化一下」** |
| 变更 | 新增 / 修改 / 删除了什么功能，逐条列 |
| 实现 | 关键做法与取舍。为什么选这个方案，放弃了什么方案 |
| 动了哪些文件 | 逐个列出，标注新增 / 修改 / 删除 |
| 验证 | 怎么确认它是对的。自检项数、实测数据、浏览器验证结果 |

模板见 `docs/devlog/README.md`。

### 什么算「一次改动」

以「一个可独立描述的目的」为界，不以提交数为界。一次修图片显示可以包含
五个提交，但只写一篇日志；同一天做了两件不相干的事，写两篇。

## 代码约定

- **注释写「为什么」，不写「做了什么」。** 尤其是阈值、正则、异常分支 ——
  凡是带具体数字的判断，注释里要有实测依据（「实测 209 篇里 91 篇会卡住」
  这种），否则后人不敢动也不知道能不能动
- **流水线自检零依赖**：`pipeline/test_pipeline.py` 用 assert + 文件底部的
  runner，不引入测试框架，CI 里直接 `python pipeline/test_pipeline.py`
- **改了流水线行为就要补自检**，尤其是「拦下了什么」这类安全阀逻辑
- 中文注释与文档，术语保留英文原词

## 校验器的地位

`pipeline/verify.py` 是这条流水线的安全阀。内容是生物医药 CMC 与分析方法，
AI 把某个流速、某条法规编号改错了是专业性错误而不是排版问题。

**放宽校验规则前先问：这条规则当初是为了拦什么？** 拦不住就别上线。
